
// Let gradle know we're using Scala
apply plugin: 'scala'

// Define our source tree
sourceSets.main.scala.srcDir 'src'

repositories {
    jcenter()
}

dependencies {
	compile group: 'com.typesafe.play', name: 'play-json_2.11', version: '2.5.12'
}
